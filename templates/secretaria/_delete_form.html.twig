<form method="post" class="delete form" action="{{ path('secretaria_delete', {'id': secretarium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secretarium.id) }}">
    <button class="ui red button"><i class="trash icon"></i> Delete</button>
</form>

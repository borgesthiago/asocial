{% extends 'base.html.twig' %}

{% block title %}Novo Familiar{% endblock %}

{% block body %}
<div class="espaco">
 <div class="ui grid">
        <div class="row">
            <div class="eight wide column">
                <h3>Adicionar Familiar</h3>
            </div>
            <div class="eight wide column">

            </div>
        </div>
    </div>
    {{ include('familia/_form.html.twig') }}
    
    <a href="{{ path('familia_index') }}"><button type="button" class="ui button secondary"><i class="fa fa-arrow-circle-left"></i> Voltar</button></a>

</div>
{% endblock %}

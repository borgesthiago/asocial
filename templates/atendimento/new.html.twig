{% extends 'base.html.twig' %}

{% block title %}Novo Atendimento{% endblock %}

{% block body %}
<div class="espaco">
    <div class="ui grid">
        <div class="row">
            <div class="eight wide column">
                <h3>Adicionar Atendimento</h3>
            </div>
            <div class="nine wide column">

    <a href="{{ path('atendimento_index') }}"><button type="button" class="ui button secondary"><i class="fa fa-arrow-circle-left"></i> Voltar</button></a>
            </div>
    </div>
</div>
    {{ include('atendimento/_form.html.twig') }}
</div>
{% endblock %}
{% extends 'base.html.twig' %}

{% block title %}Novo Usuário{% endblock %}

{% block body %}
<div class="espaco">
    <div class="ui grid">
        <div class="row">
            <div class="eight wide column">
                <h3>Adicionar Usuário</h3>
            </div>
            <div class="nine wide column">

    <a href="{{ path('usuario_index') }}"><button type="button" class="ui button secondary"><i class="fa fa-arrow-circle-left"></i> Voltar</button></a>
            </div>
    </div>
</div>
    {{ include('usuario/_form.html.twig') }}

</div>
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('assets/js/jquery.mask.min.js') }}"></script>
{% endblock %}
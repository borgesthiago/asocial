{% form_theme form 'form/form_div.html.twig' %}
<div class="ui grid">
    <div class="column">
        {{ form_start(form, {'attr': {'class': 'ui form'}}) }}
        <div class="fields">
            {{ form_row(form.nome, {'attr': {'data-row-class': 'seven wide'}}) }}
        </div>
        <div class="fields">
            {{ form_row(form.status, {'attr': {'data-row-class': 'two wide'}}) }}
            {{ form_row(form.secretaria, {'attr': {'data-row-class': 'five wide'}}) }}
        </div>
        <div class="fields">
            <div class="ui right floated buttons">
                <button class="ui primary button"><i class="save icon"></i>{{ button_label|default('Salvar') }}</button>
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}